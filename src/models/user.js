import userService from '../services/user'
import router from 'umi/router';

export default {
    namespace: 'user',

    state: {
        user: null,
    },

    effect: {
    },
    reducers: {
    },
}